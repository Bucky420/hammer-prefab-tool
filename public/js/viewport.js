import { moveVertices } from "./geometry-model.js";
import { applySelection, selectByShape } from "./selection.js";
import { roundToGrid } from "./grid.js";

const COLORS = { grid: "#1b2b40", majorGrid: "#2b405b", line: "#6b86a5", axis: "#c74b61", vertex: "#d5e4f5", selected: "#ffcf65", active: "#66dde3" };
const AXES = { top: ["x", "y", "z"], front: ["x", "z", "y"], side: ["y", "z", "x"] };

export class Viewport {
  constructor(canvas, kind, state, onChange = () => {}) { this.canvas = canvas; this.kind = kind; this.state = state; this.onChange = onChange; this.ctx = canvas.getContext("2d"); this.scale = 1; this.offset = { x: 0, y: 0 }; this.drag = null; this.bindPaintSelection(); this.bind(); new ResizeObserver(() => this.draw()).observe(canvas); }
  axes() { return AXES[this.kind]; }
  plane(vertex) { const [horizontal, vertical] = this.axes(); return { x: vertex[horizontal], y: -vertex[vertical] }; }
  world(point) { const rect = this.canvas.getBoundingClientRect(), axes = this.axes(); return { [axes[0]]: (point.x - rect.width / 2 - this.offset.x) / this.scale, [axes[1]]: -(point.y - rect.height / 2 - this.offset.y) / this.scale }; }
  screen(vertex) { const point = this.plane(vertex), rect = this.canvas.getBoundingClientRect(); return { x: rect.width / 2 + point.x * this.scale + this.offset.x, y: rect.height / 2 + point.y * this.scale + this.offset.y }; }
  vertexPoints() { return this.state.brushes.flatMap(brush => brush.vertices.map((vertex, index) => ({ ...this.screen(vertex), id: `${brush.id}:v:${index}` }))); }
  brushAt(x, y) { return this.state.brushes.find(brush => { const points = brush.vertices.map(vertex => this.screen(vertex)); const minX = Math.min(...points.map(point => point.x)) - 5, maxX = Math.max(...points.map(point => point.x)) + 5, minY = Math.min(...points.map(point => point.y)) - 5, maxY = Math.max(...points.map(point => point.y)) + 5; return x >= minX && x <= maxX && y >= minY && y <= maxY; }); }
  selectionOperation(event) { return event.altKey ? "remove" : event.ctrlKey || event.metaKey ? "toggle" : event.shiftKey ? "add" : "replace"; }
  bindPaintSelection() {
    this.canvas.addEventListener("pointerdown", event => { if (event.button !== 0) return; const nearest = this.vertexPoints().find(point => Math.hypot(point.x - event.offsetX, point.y - event.offsetY) <= 9); if (!nearest || (!event.altKey && !event.ctrlKey && !event.shiftKey && this.state.selection.has(nearest.id))) return; event.preventDefault(); event.stopImmediatePropagation(); this.canvas.setPointerCapture(event.pointerId); this.drag = { type: "paint", radius: 14, operation: this.selectionOperation(event), painted: new Set([nearest.id]) }; this.state.selection = applySelection(this.state.selection, [nearest.id], this.drag.operation); this.onChange(true); this.draw(); }, true);
    this.canvas.addEventListener("pointermove", event => { if (this.drag?.type !== "paint") return; event.preventDefault(); event.stopImmediatePropagation(); const selected = selectByShape(this.vertexPoints(), { x: event.offsetX, y: event.offsetY, r: this.drag.radius }, "circle").filter(point => !this.drag.painted.has(point.id)); selected.forEach(point => this.drag.painted.add(point.id)); this.state.selection = applySelection(this.state.selection, selected.map(point => point.id), this.drag.operation); this.onChange(true); this.draw(); }, true);
    this.canvas.addEventListener("pointerup", event => { if (this.drag?.type !== "paint") return; event.preventDefault(); event.stopImmediatePropagation(); this.drag = null; this.onChange(true); this.draw(); }, true);
  }
  bind() {
    this.canvas.addEventListener("pointerdown", event => { if (event.button === 1 || (event.button === 0 && this.state.tool === "pan")) { this.canvas.setPointerCapture(event.pointerId); this.drag = { type: "pan", x: event.offsetX, y: event.offsetY }; return; } if (event.button !== 0) return; const nearest = this.vertexPoints().find(point => Math.hypot(point.x - event.offsetX, point.y - event.offsetY) <= 9), brush = this.brushAt(event.offsetX, event.offsetY); if (this.state.mode === "vertex" && nearest && this.state.selection.has(nearest.id) || this.state.mode !== "vertex" && brush) { const original = new Map(); const movingBrushes = this.state.mode === "vertex" ? new Set() : new Set([...this.state.brushSelection, brush?.id]); for (const item of this.state.brushes) item.vertices.forEach((vertex, index) => { const id = `${item.id}:v:${index}`; if (this.state.mode === "vertex" ? this.state.selection.has(id) : movingBrushes.has(item.id)) original.set(id, { ...vertex }); }); if (brush && this.state.mode !== "vertex") this.state.brushSelection = new Set([brush.id]); this.canvas.setPointerCapture(event.pointerId); this.drag = { type: "move", start: { x: event.offsetX, y: event.offsetY }, original }; return; } this.canvas.setPointerCapture(event.pointerId); this.drag = { type: "box", x: event.offsetX, y: event.offsetY, currentX: event.offsetX, currentY: event.offsetY, operation: this.selectionOperation(event) }; this.draw(); });
    this.canvas.addEventListener("pointermove", event => { if (!this.drag) return; if (this.drag.type === "pan") { this.offset.x += event.offsetX - this.drag.x; this.offset.y += event.offsetY - this.drag.y; this.drag.x = event.offsetX; this.drag.y = event.offsetY; this.draw(); return; } if (this.drag.type === "move") { const start = this.world(this.drag.start), after = this.world({ x: event.offsetX, y: event.offsetY }), axes = this.axes(), delta = { x: 0, y: 0, z: 0 }; delta[axes[0]] = roundToGrid(after[axes[0]] - start[axes[0]], this.state.grid); delta[axes[1]] = roundToGrid(after[axes[1]] - start[axes[1]], this.state.grid); for (const brush of this.state.brushes) brush.vertices.forEach((vertex, index) => { const original = this.drag.original.get(`${brush.id}:v:${index}`); if (original) Object.assign(vertex, original); }); if (this.state.mode === "vertex") moveVertices(this.state.brushes, this.state.selection, delta, this.state.grid, false); else moveVertices(this.state.brushes, this.state.brushSelection, delta, this.state.grid, false); this.onChange("preview"); this.draw(); return; } this.drag.currentX = event.offsetX; this.drag.currentY = event.offsetY; this.draw(); });
    this.canvas.addEventListener("pointerup", () => { if (!this.drag) return; if (this.drag.type === "box") { const minX = Math.min(this.drag.x, this.drag.currentX), maxX = Math.max(this.drag.x, this.drag.currentX), minY = Math.min(this.drag.y, this.drag.currentY), maxY = Math.max(this.drag.y, this.drag.currentY); if (this.state.mode === "vertex") { const selected = selectByShape(this.vertexPoints(), { x: this.drag.x, y: this.drag.y, w: this.drag.currentX - this.drag.x, h: this.drag.currentY - this.drag.y }, "box"); this.state.selection = applySelection(this.state.selection, selected.map(point => point.id), this.drag.operation); } else { const selected = this.state.brushes.filter(brush => { const points = brush.vertices.map(vertex => this.screen(vertex)); return points.some(point => point.x >= minX && point.x <= maxX && point.y >= minY && point.y <= maxY); }).map(brush => brush.id); this.state.brushSelection = applySelection(this.state.brushSelection, selected, this.drag.operation); } this.onChange(true); } else if (this.drag.type === "move") this.onChange(false); this.drag = null; this.draw(); });
    this.canvas.addEventListener("wheel", event => { event.preventDefault(); this.zoomAt(event.offsetX, event.offsetY, event.deltaY > 0 ? 1 / 1.2 : 1.2); }, { passive: false });
  }
  zoomAt(x, y, factor) { const rect = this.canvas.getBoundingClientRect(), worldX = (x - rect.width / 2 - this.offset.x) / this.scale, worldY = (y - rect.height / 2 - this.offset.y) / this.scale, nextScale = Math.max(.12, Math.min(4, this.scale * factor)); if (nextScale === this.scale) return; this.scale = nextScale; this.offset = { x: x - rect.width / 2 - worldX * nextScale, y: y - rect.height / 2 - worldY * nextScale }; this.draw(); }
  focus() { const vertices = this.state.brushes.flatMap(brush => brush.vertices); if (!vertices.length) { this.scale = 1; this.offset = { x: 0, y: 0 }; this.draw(); return; } const points = vertices.map(vertex => this.plane(vertex)), minX = Math.min(...points.map(point => point.x)), maxX = Math.max(...points.map(point => point.x)), minY = Math.min(...points.map(point => point.y)), maxY = Math.max(...points.map(point => point.y)), rect = this.canvas.getBoundingClientRect(); this.scale = Math.max(.12, Math.min(4, (rect.width - 72) / Math.max(1, maxX - minX), (rect.height - 72) / Math.max(1, maxY - minY))); this.offset = { x: -(minX + maxX) * this.scale / 2, y: -(minY + maxY) * this.scale / 2 }; this.draw(); }
  centerWorld() { this.offset = { x: 0, y: 0 }; this.draw(); }
  drawGrid(context, width, height) { let spacing = this.state.grid; const minX = (-width / 2 - this.offset.x) / this.scale, maxX = (width / 2 - this.offset.x) / this.scale, minY = (-height / 2 - this.offset.y) / this.scale, maxY = (height / 2 - this.offset.y) / this.scale; while (spacing * this.scale < 2 || Math.max((maxX - minX) / spacing, (maxY - minY) / spacing) > 180) spacing *= 2; const majorSpacing = spacing * 8, firstX = Math.floor(minX / spacing), lastX = Math.ceil(maxX / spacing), firstY = Math.floor(minY / spacing), lastY = Math.ceil(maxY / spacing); context.lineWidth = .5; for (let index = firstX; index <= lastX; index++) { const x = index * spacing, screenX = width / 2 + x * this.scale + this.offset.x; context.strokeStyle = index % 8 === 0 ? COLORS.majorGrid : COLORS.grid; context.beginPath(); context.moveTo(screenX, 0); context.lineTo(screenX, height); context.stroke(); } for (let index = firstY; index <= lastY; index++) { const y = index * spacing, screenY = height / 2 - y * this.scale + this.offset.y; context.strokeStyle = index % 8 === 0 ? COLORS.majorGrid : COLORS.grid; context.beginPath(); context.moveTo(0, screenY); context.lineTo(width, screenY); context.stroke(); } context.lineWidth = 1; }
  draw() { const context = this.ctx, rect = this.canvas.getBoundingClientRect(), width = this.canvas.width = rect.width, height = this.canvas.height = rect.height; context.fillStyle = "#111824"; context.fillRect(0, 0, width, height); context.lineWidth = 1; this.drawGrid(context, width, height); const origin = this.screen({ x: 0, y: 0, z: 0 }); context.strokeStyle = COLORS.axis; context.beginPath(); context.moveTo(0, origin.y); context.lineTo(width, origin.y); context.moveTo(origin.x, 0); context.lineTo(origin.x, height); context.stroke(); for (const brush of this.state.brushes) for (const face of brush.faces) { context.beginPath(); face.forEach((index, faceIndex) => { const point = this.screen(brush.vertices[index]); faceIndex ? context.lineTo(point.x, point.y) : context.moveTo(point.x, point.y); }); context.closePath(); context.strokeStyle = this.state.brushSelection?.has(brush.id) ? COLORS.selected : COLORS.line; context.stroke(); } for (const point of this.vertexPoints()) { context.fillStyle = this.state.selection.has(point.id) ? COLORS.selected : COLORS.vertex; context.beginPath(); context.arc(point.x, point.y, this.state.selection.has(point.id) ? 4 : 2.5, 0, Math.PI * 2); context.fill(); } if (this.drag?.type === "circle") { context.strokeStyle = COLORS.active; context.setLineDash([4, 3]); context.beginPath(); context.arc(this.drag.x, this.drag.y, Math.hypot(this.drag.currentX - this.drag.x, this.drag.currentY - this.drag.y), 0, Math.PI * 2); context.stroke(); context.setLineDash([]); } }
}
